# requirements for basic usage
numpy
Pillow
PyYAML
torch
torchvision

# requirements for training
wandb
tqdm

# requirements for local DINOv3 backbone loading
ftfy
omegaconf
regex
scikit-learn
submitit
termcolor
torchmetrics

# requirements for video inference
opencv-python

# Optional TensorRT profiling/export dependencies live in requirements-tensorrt.txt.
# Optional RT-DETRv4 frozen-backbone dependencies live in requirements-rtdetrv4.txt.
